## stub-extension:
Base code for making a communication extension for gdbserver.

## midi-extension:
Example extension that makes gdbserver use the midi ports instead of the serial port to communicate.

